// Fix for the doctor dashboard\nvar fixErrorLine = function() {\n  if (document.getElementById('footer-user-roles')) {\n    var userRoles = (frappe.session && frappe.session.user_roles && Array.isArray(frappe.session.user_roles)) ? frappe.session.user_roles.join(', ') : 'User';\n    .html('<i class="fa fa-shield"></i> ' + userRoles);\n  }\n};\n.ready(function() {\n  // Delay execution to ensure session data is loaded\n  setTimeout(fixErrorLine, 1000);\n});
